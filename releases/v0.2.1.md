# 0.2.1 — Guided native setup

Source-record date: **2026-09-10**. This is the date recorded in the publisher's note, not an inferred public upload date.

Historical note record. Availability of an archived binary is not asserted.

## Recorded changes

- Optional first-install Agent 589 setup includes GPU, RAM, storage, engine and model readiness guidance.
- Native runtime/model paths follow the selected app data root.
- Release inventories cover UI assets and named native resources without an environment-variable verification bypass.
- Upgrades retain choices instead of repeating setup; uninstall no longer offers app-data deletion.
- Setup completes only when the exact model responds; failed/skipped steps do not silently advance and existing files are reused.

## Data and provenance

Normal installer upgrades replace program files and preserve app data, Main projects and scratch work. Keep independent backups; uninstalling is not complete erasure.

Curated from the publisher's frozen `v0.2.1.md` record. Details are summarized for users, without converting historical implementation notes into universal current guarantees. Later versions can supersede earlier workflows. The original note's hash is in [Release provenance](../docs/RELEASE_PROVENANCE.md).

[All versions](../CHANGELOG.md) · [Known limitations](../docs/KNOWN_LIMITATIONS.md)

