# 0.2.21 — Clone write lease and Console auto-scroll

Source-record date: **2026-09-24**.

**Unsigned early access — manual installation and updates.** [Download and verify](../docs/DOWNLOADS.md). Historical installers keep their original versions and notes.

## Recorded changes

- Saving **Read and edit** (or **Read only**) updates the selected connection profile and related base/connection rows together, so Workspace agents regain create/edit tools on the clone instead of reporting that no file-write or patch tool is available.
- Re-selecting an existing provider connection profile refreshes clone access from the base account, healing stale Read-only connection rows.
- Host prompts only claim a writable clone when the saved lease actually allows mutation; otherwise agents are told to enable Read and edit in Providers.
- Workspace Console chat follows new agent output to the bottom while work is in progress, with a jump control if you scroll up.

## Data and provenance

Upgrades replace installed program files and preserve separate user data, projects, Main folders, downloaded models and saved choices. Native models remain optional downloads. WebView2 may still need internet if missing.

## Validation scope

Focused unit coverage for related-profile permission sync and lease-aligned guidance passed. Package identity and checksum are recorded after the installer build. Separate-device installation remains unverified here.

Final package identity: `I.M.P.E.R.I.U.M_0.2.21_x64-setup.exe`, **34203636** bytes, SHA-256 `8221ad315628e54a3127be271cf9cbe06cd1fb450fba76eba2ecfb047526bcde`.
