# 0.2.9 — CPU Mini and durable activity

Source-record date: **2026-09-13**. This is the date recorded in the publisher's note, not an inferred public upload date.

Historical note record. Availability of an archived binary is not asserted.

## Recorded changes

- Bundled CPU-sized Agent 589 Mini adds payload integrity checks and offline first-use setup.
- Durable task activity/report records span work environments with separate Studio/Blueprint histories and scoped controls.
- Shadow Verify identifies filler overlap with edited regions; project artwork, file counts and scan labels match the theme/current project.
- Security/recovery evidence is authenticated and project-bound; editable legacy receipts are not automatically promoted to trusted status.
- Terminal/tool operations gain bounded output and cleanup; native setup has distinct Mini, Agent 589 and Core identities.
- Fixes address cancellation/output/follow-up races, Clone/patch recovery, login evidence, skill integrity and guarded paths.
- Dependency/editor compatibility, long paths, Git apply evidence and process-start timeouts improve.
- Updates ask users to save/close the app; silent updates refuse a running app rather than broadly killing processes.
- Mini receives the current Studio task separately from background context and avoids unrelated export/media instructions.

## Data and provenance

Normal installer upgrades replace program files and preserve app data, Main projects and scratch work. Keep independent backups; uninstalling is not complete erasure.

Curated from the publisher's frozen `v0.2.9.md` record. Details are summarized for users, without converting historical implementation notes into universal current guarantees. Later versions can supersede earlier workflows. The original note's hash is in [Release provenance](../docs/RELEASE_PROVENANCE.md).

[All versions](../CHANGELOG.md) · [Known limitations](../docs/KNOWN_LIMITATIONS.md)

